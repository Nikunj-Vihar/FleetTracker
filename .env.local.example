# Copy this file to .env.local and fill in your Supabase project credentials.
# If left unset, the app automatically falls back to a LocalStorage-backed
# mock engine so it can run fully offline (see src/lib/store.ts).

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
